export default interface Search {
    /**
     * @isString Only accept string values
     * @minLength 1 Minimum length of 1
     */
    novel_id: string
    /**
     * @isString Only accept string values
     * @minLength 1 Minimum length of 1
     */
    title: string
}
